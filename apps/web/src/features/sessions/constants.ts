/** Select placeholder for "no task" — Radix Select cannot hold an empty value. */
export const NO_TASK = "__none__";

/** A manual entry defaults to the hour before now, which is the usual case. */
export const DEFAULT_MANUAL_SESSION_MINUTES = 60;
