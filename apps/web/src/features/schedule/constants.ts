/** Row height of one hour. Tighter on phones, where vertical space is scarce. */
export const PX_PER_HOUR = 56;
export const PX_PER_HOUR_COMPACT = 44;

/** Below this a block can only fit its title; the time moves to the tooltip. */
export const MIN_BLOCK_PX = 28;

/** Width of the hour-label gutter. */
export const AXIS_WIDTH_PX = 44;

/** Click-to-add snaps to the nearest half hour. */
export const SLOT_SNAP_MINUTES = 30;

/** Default length of a block created by clicking an empty slot. */
export const DEFAULT_BLOCK_MINUTES = 60;

/** How often the current-time line re-reads the clock. */
export const NOW_LINE_TICK_MS = 30_000;
